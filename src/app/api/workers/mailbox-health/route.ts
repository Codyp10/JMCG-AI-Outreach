/**
 * Alias for plan v2.2 webhook URL (`/api/workers/mailbox-health`).
 * Same behavior as `/api/workers/mailbox-health-check`.
 */
export { maxDuration, POST } from "../mailbox-health-check/route";
